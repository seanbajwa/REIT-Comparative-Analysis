# REIT-Comparative-Analysis
# Attempting to Scrape 10Q's and folder map them conveniently on each ticker
# Need to create an interpeter from JSON (API from EDGAR) that makes these files more readable
